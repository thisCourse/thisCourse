<script id="delete_dialog" type="text/x-handlebars-template">
    
    Are you SURE you want to permanently delete
    {{#if title}}
        the entire {{ type }} entitled "{{ title }}"?
    {{else}}
        this entire {{ type }}?
    {{/if}}

</script>

