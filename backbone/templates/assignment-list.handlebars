<h2>Assignments</h2>
<ul>
    {{#each models }}
        <li id="{{ id }}"><a>{{ attributes/title }}</a></li>
    {{/each}}
</ul>

{{#if _editor }}<button class="add-button btn success">Add assignment</button>{{/if}}
